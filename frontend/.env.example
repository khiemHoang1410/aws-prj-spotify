# Copy file này thành .env và điền URL từ sst dev output
# Chạy: cp .env.example .env

# Dev: lấy từ output của "sst dev" -> api: https://xxx.execute-api.ap-southeast-1.amazonaws.com
VITE_API_URL=https://YOUR_API_ID.execute-api.ap-southeast-1.amazonaws.com
